name: React Doctor

on:
  push:
    branches: ["main"]

permissions:
  contents: write

jobs:
  doctor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node v22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Run React Doctor
        id: doctor
        run: |
<<<<<<< HEAD
          # Run diagnostic and output annotations
          npx react-doctor@latest . --project pimp --fail-on none --annotations
          
          # Capture score reliably. Using --project avoids the prompt.
          # We grab the last line which should be the score when using --score.
          SCORE=$(npx react-doctor@latest . --project pimp --score --fail-on none | tail -1 | tr -d '[:space:]')
=======
          # Run once to show diagnostics in logs and output annotations
          npx react-doctor@latest . --fail-on none -y --annotations
          
          # Run again to capture score reliably. -y skips project prompts.
          SCORE=$(npx react-doctor@latest . --score --fail-on none -y | tail -1 | tr -d '[:space:]')
>>>>>>> 1e2b27b5d0b6c7d80ee9a38c0a660e2422d4eaa1
          echo "score=$SCORE" >> "$GITHUB_OUTPUT"

      - name: Update README Badge
        run: |
          SCORE=${{ steps.doctor.outputs.score }}
          COLOR="lightgrey"
          if [[ "$SCORE" =~ ^[0-9]+$ ]]; then
            if [ "$SCORE" -gt 85 ]; then COLOR="brightgreen";
            elif [ "$SCORE" -gt 70 ]; then COLOR="green";
            elif [ "$SCORE" -gt 50 ]; then COLOR="yellow";
            elif [ "$SCORE" -gt 25 ]; then COLOR="orange";
            else COLOR="red"; fi
          else
            SCORE="0"
            COLOR="red"
          fi
          
          BADGE="[![Health Score](https://img.shields.io/badge/React_Doctor-${SCORE}%2F100-${COLOR})](https://github.com/millionco/react-doctor)"
          sed -i "s@\[\!\[Health Score\].*@${BADGE} <!-- DOCTOR_BADGE_START --><!-- DOCTOR_BADGE_END -->@g" README.md

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update react-doctor health score [skip ci]"
          file_pattern: 'README.md'
