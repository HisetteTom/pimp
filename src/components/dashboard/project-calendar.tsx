/**
 * Re-exports the default ProjectCalendar component and its associated TypeScript type definitions.
 * Serves as the main calendar entry point to organize project-wide checkpoints and tasks.
 */
export { ProjectCalendar as default } from './calendar';
export type { Task, Checkpoint, CheckpointNote } from './calendar/types';
